import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    root: {
        maxWidth: 1045,
    },
    image: {
        height: 450,
        width: 1045,
    }
});

export default useStyles;
